function Game() {
    this.check = (string) => {
        if (value1 === value2) {
            return 'Its a tie!';
        } else {
            return 'That is not the game pal, try "rock", "paper" or "scissors"';
        }
    }
}